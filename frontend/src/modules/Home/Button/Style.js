import { StyleSheet } from 'react-native'

const Styles = StyleSheet.create(
    {
        TextButton: {
            fontSize: 22,
            fontWeight: "bold",
            color:"black",
        }
    }
)
export default Styles